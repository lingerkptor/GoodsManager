listAllTag = select TagName,TagDescription,count(GID) from TAGS LEFT JOIN GOODSTAGS on TAGS.TID = GOODSTAGS.TID GROUP by TAGS.TID;
getTagsbyGID = select TagName,TagDescription from TAGS LEFT JOIN GOODSTAGS on TAGS.TID = GOODSTAGS.TID WHERE GID = ? GROUP by TAGS.TID;