countDataRow=SELECT CASE  WHEN count(GID)% ? >0 then count(GID)/ ? +1 else count(GID)/ ? END as sumPage ,count(GID) from GOODS left join Classification on GOODS.CID = Classification.CID
selectGoodsData=select GID,GNAME,PRICE,COUNT,C_NAME,DATE from GOODS left join Classification on GOODS.CID = Classification.CID 
conditionClassification= Classification.C_NAME = ?
conditionGoodsNameKeyword= GNAME like ?
conditionTagGID= GID in ( select DISTINCT GID from GOODSTAGS left join TAGS on TAGS.TID = GOODSTAGS.TID where TAGS.TagName in (
conditionDate = Date = ? 
!conditionDefaultToken= GID > 0 ORDER by GID 
!conditionCustomsToken= GID > ? ORDER by GID 
!coditionSortbyDate= DATE > ? ORDER by DATE
!coditionSortbyClass= C_NAME > ? ORDER by C_NAME
conditionCustomsToken=   ORDER by GID  
coditionSortbyDate=  ORDER by DATE
coditionSortbyClass=   ORDER by C_NAME

coditionCount=LIMIT ? offset ?